{% extends 'base.html.twig' %}

{% block title %}Modifier la fiche produit
{% endblock %}

{% block body %}
	<h1>Modification de la fiche produit</h1>

	{{ include('product/_form.html.twig', {'button_label': 'Enregistrer la modification'}) }}
	<div class="twobuttons">
		<a href="{{ path('app_product_show', {'id': product.id}) }}" class="add-button-customer">Retour au produit</a>
	</div>
{% endblock %}
